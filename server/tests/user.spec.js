import { User, db } from '../db';
import { sync } from '../../sync';

describe('getCash instance method returns current balance of user', () => {
  let user;

  it('Runs a test', () => {
    expect(true).toEqual(true);
  });
});
